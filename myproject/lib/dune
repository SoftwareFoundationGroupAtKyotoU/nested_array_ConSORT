(menhir
  (modules parser z3Parser)
)
(ocamllex
  (modules lexer z3Lexer)
)

(library
 (name myproject)
 (libraries ounit2)
 (preprocess (pps ppx_deriving.std)))

(env
 (dev
   (flags
     (:standard -warn-error -A -w -39)
   )))
