assum(a, b)
[<a:int, b:int> -> <a:int, b:int|int>]
{
    if a > 5 then {
        a
    } else {
        let d = assum(a, b) in
        d
    }
}

{
    let n = 0 in
    let arg = 5 in
    let d = assum(arg, n) in
    assert(d > 10);
    ()
}